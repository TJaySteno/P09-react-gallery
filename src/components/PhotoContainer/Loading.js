import React from 'react';

const Loading = () => (
  <li className='loading'>
    <h3>Loading...</h3>
    <p>Please wait a moment while our worker monkeys fetch your data.</p>
  </li>
);

export default Loading;
